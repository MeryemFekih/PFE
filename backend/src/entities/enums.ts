// src/auth/enums.ts

export enum IDType {
    CIN = 'CIN',
    Passport = 'Passport',
  }
  
  export enum UserType {
    student = 'student',
    alumni = 'alumni',
    professor = 'professor',
  }
  